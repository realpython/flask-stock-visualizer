# specify language
language:
  - python

# specify python versions
python:
  - "2.6"
  - "2.7"

# install dependencies
install:
  - pip install -r requirements.txt

# run tests
script:
  - python tests.py

deploy:
  provider: heroku
  api_key: e2b5aaa0cb18d81d760472099e0bf2f509a91b97
  app: flask-stock